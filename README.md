# Dist

Dist is a basic distribution template. It shows the basic settings that one distribution needs.

## Installation

    npm init -t dist

# Tip

This project is too large, so it's not recommended to clone or download the whole thing.

## What's important

`/applications` The main program source code is in the directory.Other directories are source code of BearPi
Therefore,I recommended download `/applications/*`
